server:
  port: 8010
eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false  #//是否注册
    fetchRegistry: false #是否检索服务
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/