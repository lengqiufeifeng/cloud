eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/
spring:
  application:
    name: drools-dispatch
server:
  port: 8082
zuul:
  routes:
    service-user: /user/**
    service-monitoring: /monitoring/**